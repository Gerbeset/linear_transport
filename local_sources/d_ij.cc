#include "d_ij.h"
#include <cmath>
#include <algorithm> 

double d_ij(const double U_i, const double U_j)
    {
       //return 0; 
        return 0.5*1.0; 
      // return 0.5*std::max(std::abs(U_i), std::abs(U_j)); 
    }